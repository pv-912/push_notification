{
  "homepage": "https://pv-912.github.io/reactServiceWorker/build",
  "name": "my-app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "gh-pages": "^1.1.0",
    "react": "^16.3.0",
    "react-dom": "^16.3.0",
    "react-router-dom": "^4.2.2",
    "react-scripts": "1.1.1",
    "yarn": "^1.5.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "npm run cleanup && gh-pages -d build -e build",
    "cleanup": "rm -rf node_modules/gh-pages/.cache"
  },
  "devDependencies": {
    "gh-pages": "^1.1.0"
  }
}
